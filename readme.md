Requerimentos Software - Python (habilitar add Python to PATH)
Extensão Python (VSCODE), Extensão PowerShell (VSCODE), Extensão Live Server (VSCODE)

py -m venv venv

./venv/scripts/activate

pip install -r requirements

py app.py

